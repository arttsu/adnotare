(ns adnotare.core.state.palettes-test
  (:require
   [clojure.test :refer [deftest is testing]]
   [adnotare.core.state.palettes :as state.palettes]))

(deftest first-id-test
  (testing "multiple palettes"))
